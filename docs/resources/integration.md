---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "roadie_integration Resource - roadie"
subcategory: ""
description: |-
  Manages a Roadie integration.
---

# roadie_integration (Resource)

Manages a Roadie integration.

## Example Usage

```terraform
resource "roadie_secret" "github_token" {
  ref         = "GITHUB_TOKEN"
  name        = "GitHub Token"
  description = "Personal access token for GitHub API"
  value       = var.github_token
}

resource "roadie_integration" "github" {
  name         = "GitHub"
  slug         = "github"
  type         = "scm"
  host         = "https://api.github.com"
  auth_type    = "bearer-token"
  backend_type = "http"
  enabled      = true

  auth_config = jsonencode({
    token = "$${GITHUB_TOKEN}"
  })

  depends_on = [roadie_secret.github_token]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String)
- `slug` (String)

### Optional

- `auth_config` (String) Auth configuration JSON containing ${SECRET_REF} placeholders, not raw credentials.
- `auth_type` (String)
- `backend_type` (String)
- `burst_capacity` (Number)
- `config` (String)
- `enabled` (Boolean)
- `host` (String)
- `logo_slug` (String)
- `requests_per_hour` (Number)
- `requests_per_second` (Number)
- `type` (String)

### Read-Only

- `adopted` (Boolean) Whether this resource was adopted from a pre-existing integration rather than created by Terraform.
- `created_at` (String)
- `created_by` (String)
- `id` (String) The ID of this resource.
- `updated_at` (String)
